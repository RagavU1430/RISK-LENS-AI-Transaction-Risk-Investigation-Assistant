# Copy to .env when needed. Do NOT commit real keys.
# Phase 4: Gemini investigation explanations (deterministic pipeline works without it).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite
GEMINI_TIMEOUT_SECONDS=60

# Optional overrides
APP_ENV=development
PORT=8000
